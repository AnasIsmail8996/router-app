import React from 'react';
import './Loading.css'; // Import the custom loader CSS

const Loading = () => {
  return (
    <section className="loader-parent">
      <div className="loader"></div>
    </section>
  );
};

export default Loading;
